# Irrigation Logbook

Считает расход воды по зонам полива из продолжительности и расхода линии.

## Запуск

```bash
python src/irrigation_logbook.py totals examples/sample.json
```

Локальный инструмент без внешних сервисов. Проверка: `python -m unittest discover -s tests -v`.
